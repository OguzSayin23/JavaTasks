package test.java.interviewQuestions.fromMuhtar;

import java.util.ArrayList;

public class ___String_LongestPalindrome {
     /*
    Write a program that can return the longest palindrome from a string
     */
     public static void main(String[] args) {
         String str = "palindalrometopspot";
         System.out.println(longestPalindrome(str));
     }
     public static String longestPalindrome(String S){


























//         String pal = "";
//         ArrayList<String> list = new ArrayList<>();
//         for (int i = 0; i < S.length(); i++) {
//             for (int j = i+1; j <=S.length() ; j++) {
//                 list.add(S.substring(i,j));
//             }
//         }
//
//         for (String each:list) {
//             String result = "";
//             for (int i = each.length()-1; i >=0 ; i--) {
//                 result+=each.charAt(i);
//             }
//             if (each.equalsIgnoreCase(result) && each.length()>pal.length()) {
//                 pal =each;
//             }
//         }


         return S;
     }
}
